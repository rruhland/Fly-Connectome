# Bounded local-baseline follow-up for the LC11 shadow readout

The fixed warm-voltage reference caused broad blank release because no-event T2/T2a/T3 voltages continued to vary after warmup. Test **one** local temporal-context correction on the same measured partial LC11 motif: each source maintains its own exponential voltage baseline with a 250 ms time constant, already used for the earlier opt-in Mi4 graded-release investigation. Initialize this trace from the no-event warmup, then update it each tick from that source's own voltage. Release is `min(f, f × max(0, voltage − moving_baseline)/0.003)`, with the same `f = 0.05, 0.10, 0.25`, same sign/contact/delay rules, and same x=18 ON/OFF dot calibration and stop thresholds as [the fixed-reference protocol](2026-09-23-lc11-shadow-readout-protocol.md). There is no event label, matched-blank value, or future observation in the release mechanism.

Only if both polarities pass at x=18, freeze the smallest fraction and run the same x=46, opposite-direction, bar, and static controls. Record the per-tick release/current/spikes and blank drift as before. Compare the blank rate and evoked-current contrast directly with the fixed-reference run. If this single local baseline fails, stop this partial-LC11 route before another gain or time-constant sweep. The outcome will indicate whether the remaining limitation is temporal signal extraction from autonomous upstream activity, rather than missing anatomical LC11 edges alone. This stays entirely opt-in; no in-place architecture or learning rule changes.
