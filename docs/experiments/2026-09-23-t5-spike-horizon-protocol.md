# Fixed 8/16/32 ms horizon check for local T5 spikes

The [next-frame T5 spike target](2026-09-23-t5-future-spike-findings.md) is motion-specific but not reliably predictable at +8 neural ticks. Keep the same frozen 50 ms opt-in circuit, 18 camera frames, six position/speed cases, matched moving/static/bright-blank controls, and forecast issue boundaries in frames 3–13. Without changing or training any weight, test target windows of exactly eight neural ticks starting at **+8, +16, and +32 ticks** after each boundary. The +32 window still fits within the 144-tick replay. Read the same causal pending T5 order current and preceding-eight-tick spike count at issue time.

For each global horizon and preferred T5c/up and T5d/down pair, report future-spike event count/prevalence, order event/quiet means, order ROC AUC, recent-spike persistence AUC, and static/blank target prevalence at all six cases. The fixed screen is the same as the original preflight: at least ten events; event-order mean at least twice quiet; order AUC ≥0.70 and ≥0.05 above persistence; static event prevalence less than half preferred motion; blank prevalence <0.001. A horizon is a candidate only if it passes **all six** position/speed cases for both subtypes. This is a read-only target-capacity audit, not a horizon tuning run for learning. If no horizon qualifies, stop shifting the window on this setup and reassess what local state or objective should be learned.
