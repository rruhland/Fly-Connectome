# Geometry-routed, opt-in CT1 shadow on frozen T5 motion

The [CT1 synapse-offset audit](2026-09-23-ct1-offset-findings.md) found opposed null-side vertical input offsets for T5c and T5d. Test one fixed, separate geometry-routed shadow. Keep the pinned checkpoint, production graph, opt-in Tm9 release/T5 order current, measured threshold-three Tm1/Tm9→CT1 inputs, positive source/GABA-negative output signs, two one-tick delays, 5 ms CT1 current, 250 ms blank baseline, p99 blank floor, scale 0.02, and cap 0.10 exactly as in the earlier [column-shadow protocol](2026-09-23-ct1-column-shadow-protocol.md). No training or parameter search.

Use each deduplicated CT1→T5c/d synapse pair as one measured output contact. Assign it to the inferred optic column of the nearest **eligible threshold-three** Tm1/Tm9 input synapse on CT1 in 3D. Aggregate contacts by target T5 body and assigned input column, preserving all **37,186** measured T5c/d output contacts and the original graph gain. This is an opt-in local-process approximation; Euclidean nearest sites do not establish electrical branch identity. Do not insert CT1 into the production graph or create any non-CT1 input/output contact.

Replay exactly the earlier frozen y=10/y=22 and withheld y=16 speed-one/two up/down, blank, and static cases, with off/on from the same calibrated state. The off arm must reproduce the earlier column-shadow off counts, since its input and calibration are identical. Apply the same eight-part rescue gate: for both T5c and T5d in every condition, the CT1 current must be stronger on null motion, null spikes must fall at least 10%, preferred spikes may fall no more than 10%, direction sign must survive, and blank spikes may not increase. Report static responses and source release occupancy. If the fixed geometry route fails, stop without a radius/k/gain sweep. A pass would still require an online-learning test and explicit approval before any in-place architecture change.
