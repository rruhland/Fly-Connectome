# Frozen inhibitory-current timing at the T5 motion stage

Use the unchanged pinned checkpoint and opt-in Tm9 graded release/T5 local-order current from the axis-aligned screen. No training, changed weights, new edges, transmitter-sign changes, or altered delays. Replay the existing vertical moving bar at y=10/y=22, speed one, and withheld y=16 at speeds one/two. Include bright blank and static-bar controls. Opposite directions must have equal event-camera counts. Use the same blank-calibrated network state for every trial.

Select T5c/T5d cells by the existing anatomy-to-retina local-column map for each bar position. Read LPi34, LPi43, and VS as downstream comparison pools. At every simulation tick, capture actual signed predictive excitation/inhibition, feedforward current and arrivals, T5 local current, and soma spikes. Verify that the signed stores sum to the physical predictive current at these non-context targets. Summarize ticks 24–119, per-cell null-minus-preferred inhibitory current (T5c/LPi34 prefer up; T5d/LPi43 prefer down), the fraction of cells with null-greater inhibition, and within-frame timing (eight ticks/frame) of inhibitory current and spikes. Report predictive inhibitory/positive arrival counts separately. VS has no preassigned vertical preferred direction; retain its up/down data without choosing a preferred sign post hoc.

A candidate *existing* null-suppression channel requires null-greater inhibitory current for T5c or T5d at both calibration positions and both holdouts, in a majority of locally selected cells with nonzero current, with inhibitory arrival or current preceding rather than following the relevant response. Failing this screen does not disprove CT1 suppression because the measured CT1 route is absent from this checkpoint. A passing screen would still be a frozen dynamics observation, not a local-plasticity result. Do not promote a production change from this audit alone.
