# Motion-bearing T5 output and local downstream credit

The opt-in T5 temporal current produced held-out vertical motion tuning in T5c/d spikes, but training predictive edges *entering T5* to forecast raw afferent arrivals barely improved event error. Before modifying any learning objective, audit the measured downstream graph for cells that receive T5c/d feedforward spikes and also possess predictive afferents. Focus on subtype-associated LPi34 and LPi43 and the mixed LPi3412 population, retaining every measured edge and fixed transmitter sign. Report target-cell counts, T5c/d edge/contact mass, presynaptic signs, predictive incoming edges/contact mass, and most common predictive source classes. A topology result only identifies plausible local targets; a frozen dynamic probe must show actual direction-bearing arrivals before training them.
