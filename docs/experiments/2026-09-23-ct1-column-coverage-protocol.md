# CT1 partner-column coverage before a local-compartment experiment

Read the pinned T5 roster and original MaleCNS body-to-body contacts. Use only CT1_L body 10009, selected right-side Tm1/Tm9→CT1 contacts, and CT1→selected T5 contacts. Put Tm1/Tm9 in the existing retina column map using their anatomical hex annotations; place T5 by the previously used Tm1/Tm2-afferent weighted inference. Preserve all measured contact counts and report contacts both before and after the three-contact edge threshold. This is an anatomical coverage audit, not a neural simulation or a new graph.

For T5c/d separately, report the fraction of CT1 output contact mass in columns containing CT1 input from Tm1, Tm9, or either; repeat with an immediate hex-neighbor allowance. Report the same fractions within y=10/y=22 calibration fields and withheld y=16 field. Record how much partner contact mass cannot be assigned to a column. A body-level approximation is eligible for an **opt-in** local CT1 test only if at least 80% of selected T5c/d output contact mass at each field has a same-or-neighbor Tm1/Tm9 input, both source classes contribute in the withheld field, and missing-column mass is below 20%. This screen measures possible local coverage. It cannot establish that individual synapses occupy the same CT1 process; that needs the MaleCNS synapse-partner coordinates or a separate morphology audit. No in-place graph or dynamics change follows from a pass.
