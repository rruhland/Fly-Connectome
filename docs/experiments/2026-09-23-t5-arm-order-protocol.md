# Frozen T5 afferent-order diagnostic

The pinned graph's Tm1/Tm2/Tm4/Tm9 afferents into T5 are excitatory and spatially offset. Tm4/Tm9 show local OFF source activity, but the prior opt-in rest-current recovery (`Tm4=Tm9=0.85`) did not yield stable T5 spike preference. This read-only diagnostic tests whether their **measured arrival timing** contains a simple local direction cue that the point-soma sum discards. It changes no edge, sign, delay, weight, synapse kernel, plasticity, or production profile.

Replay the frozen checkpoint with only the previously calibrated Tm4/Tm9 rest currents of 0.85, as in the motion-stage recovery report. Warm 500 ticks and restore the same state before each matched blank, right/left OFF moving bar, and static OFF bar at x=18 and x=46, using 18 frames and eight neural ticks/frame. Instrument the existing feedforward arrivals into every T5 cell as separate causal 5 ms current traces for Tm1, Tm2, Tm4, and Tm9. Subtract the matched blank trace. T5 receptive groups use the existing contact-weighted Tm1/Tm2 afferent-column inference; group T5c and T5d locally at each location. No source column, motion label, or score enters the network.

Use one fixed **8-tick** temporal-order diagnostic, corresponding to one camera-frame interval: for each T5 cell, `order(t) = Tm4(t−8)·Tm9(t) − Tm9(t−8)·Tm4(t)` on blank-subtracted signed arm currents. Sum over transit ticks 24–119. Report right, left, and static per-cell score distributions; Tm4/Tm9 arm modulation and T5 spike excess; and right-minus-left contrasts for T5c/d at both locations. The diagnostic passes only if each subtype's right-minus-left mean score has the same nonzero sign at both locations, T5c and T5d have opposite signs at each location, the per-cell median agrees with the subtype mean, and each direction contrast exceeds the same subtype's static-score magnitude at both locations. A pass would justify a bounded local temporal/compartment mechanism experiment, not production promotion. A fail means this specific order cue is not sufficient; do not search lags or fit a direction decoder on these four conditions.
